' With no router to call handleFocus, the scene lands initial focus once the UI
' exists. That single call is the only focus code in the whole demo.
sub init()
    m.top.findNode("content").callFunc("focusDefault")
end sub
